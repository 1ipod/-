# Yet Another Bluesky Introspector
This is a thing that lets you monitor labelers! 

## Setup
Install Vlang and python
clone the repo
cd into the repo
```
python -m venv .
pip install -r requirements.txt
python labelcrawler.py &
v run .
```

